*** a/src/main.rs
--- b/src/main.rs
@@
 fn help() {
     println!("Oxid 0.5.2");
     println!("Usage:");
     println!("  oxid run <file.ox>");
     println!("  oxid script <name> [args...]");
+    println!("  oxid bootstrap");
+    println!("  oxid frontend");
+    println!("  oxid diagnose");
+    println!("  oxid lint");
+    println!("  oxid emit");
+    println!("  oxid module");
+    println!("  oxid syntax");
+    println!("  oxid self-host");
     println!("  oxid check <file.ox>");
     println!("  oxid repl");
@@
         Some("script") => match args.get(2) {
             Some(name) => run_manifest_script(Path::new("."), name, &args[3..]),
             None => Err("`oxid script` requires a script name".to_string()),
         },
+        Some("bootstrap") => run_file(Path::new("tools/bootstrap.ox"), &mut interp),
+        Some("frontend") => run_file(Path::new("tools/frontend.ox"), &mut interp),
+        Some("diagnose") => run_file(Path::new("tools/diagnose.ox"), &mut interp),
+        Some("lint") => run_file(Path::new("tools/lint.ox"), &mut interp),
+        Some("emit") => run_file(Path::new("tools/emit.ox"), &mut interp),
+        Some("module") => run_file(Path::new("tools/module.ox"), &mut interp),
+        Some("syntax") => run_file(Path::new("tools/syntax.ox"), &mut interp),
+        Some("self-host") => run_file(Path::new("tools/self_host.ox"), &mut interp),
         Some("check") => match args.get(2) {
             Some(file) => {
                 let path = Path::new(file);
