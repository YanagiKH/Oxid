use "../stdlib/prelude.ox";

fn main() {
    print "Oxid package workflow preview";
    print "Use `oxid script`, `oxid add`, and manifest scripts for package automation.";
    print "Import `stdlib/prelude.ox` in fresh projects.";
}
