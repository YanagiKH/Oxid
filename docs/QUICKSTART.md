# Quickstart

1. Build the runtime with Rust.
2. Run `oxid new demo`.
3. Edit `src/main.ox`.
4. Use `oxid run src/main.ox` or `oxid script run`.
5. Use `oxid fmt`, `oxid doctor`, and `oxid clean` during development.
