# Language Stats Goal

This repository is intentionally arranged so the Oxid source tree is large enough to dominate the visible language composition on GitHub.

The Rust code remains small and focused on bootstrap, parser, runtime, and native bridging.
The user-facing workflow, standard library, examples, scripts, and package previews are written in Oxid.
