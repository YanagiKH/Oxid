# Packages

`oxid.toml` now supports:

- `project.name`
- `project.version`
- `project.entry`
- `[scripts]`
- `[dependencies]`
- `[features]`

`oxid add <name> <target>` updates the dependency section automatically.
